server {
    listen 80;

    location / {
        root    /usr/share/nginx/html;
        index   index.html index.htm;
        # 👇 [핵심] S3의 '오류 문서' 설정과 같은 역할입니다!
        # 들어온 주소($uri)에 파일이 없으면 무조건 index.html을 줘라.
        try_files $uri $uri/ /index.html;
    }
}
